import { Meta, Title, Story } from '@storybook/addon-docs/blocks';
import { En, IqvizyonCanvas, Tr } from '@iqvizyonui/react-storybook-addon';
import { Text } from '@iqvizyonui/react-components';
import { Typography } from './ThemeTypography.stories';
import { Example } from './Example.stories';
import { Composing } from './Composing.stories';
import { MixedStyles } from './MixedStyles.stories';

<Meta title="Theme/Typography" component={Typography} />

<En>

<Title>Typography</Title>

Typography style is represented by a set of tokens instead of an individual token. The tokens are used to create and share a consistent look and feel.

> **ℹ️ &nbsp;This page guides you on how to fully leverage the tokens to create a consistent typography system.<br />
> To take full advantage of the typography system, you should also read the [Text component documentation](./?path=/docs/components-text--docs).**

<IqvizyonCanvas>
  <Typography />
</IqvizyonCanvas>

## How to use

Using typography styles is as simple as creating a custom style with any component. Simply import `typographyStyles` from the main `@iqvizyonui/react-components` package and use it in your custom style:

```tsx
import * as React from 'react';
import { makeStyles, typographyStyles } from '@iqvizyonui/react-components';

const useStyles = makeStyles({
  text: typographyStyles.title2,
});

export const Example = () => {
  const styles = useStyles();

  return <span className={styles.text}>Text using tokens</span>;
};
```

<IqvizyonCanvas>
  <Example />
</IqvizyonCanvas>

## Composing Tokens

Besides using typography styles, you can still use individual typography tokens:

```tsx
import * as React from 'react';
import { makeStyles, tokens } from '@iqvizyonui/react-components';

const useStyles = makeStyles({
  text: {
    fontFamily: tokens.fontFamilyMonospace,
    fontSize: tokens.fontSizeBase600,
    fontWeight: tokens.fontWeightBold,
    lineHeight: tokens.lineHeightBase600,
  },
});

export const Composing = () => {
  const styles = useStyles();

  return <span className={styles.text}>Custom text using only tokens</span>;
};
```

<IqvizyonCanvas>
  <Composing />
</IqvizyonCanvas>

## Mixing tokens and custom styles

To mix tokens and custom styles, spread typography styles into the style object and combine them with another CSS properties:

```tsx
import * as React from 'react';
import { makeStyles, typographyStyles, Text } from '@iqvizyonui/react-components';

const useStyles = makeStyles({
  title: typographyStyles.title2,

  paragraph: {
    ...typographyStyles.body1,
    letterSpacing: '0.0675em',
    fontStyle: 'italic',
  },
});

export const MixedStyles = () => {
  const styles = useStyles();

  return (
    <div>
      <Text as="h1" block className={styles.title}>
        Using Title 2 tokens
      </Text>

      <Text as="p" block className={styles.paragraph}>
        I'm a paragraph using Body 1 tokens and customized styles
      </Text>
    </div>
  );
};
```

<IqvizyonCanvas>
  <MixedStyles />
</IqvizyonCanvas>

</En>

<Tr>

<Title>Tipografi</Title>

Tipografi stili, tek bir belirteç yerine bir belirteç kümesiyle temsil edilir. Bu belirteçler, tutarlı bir görünüm oluşturmak ve paylaşmak için kullanılır.

> **ℹ️ &nbsp;Bu sayfa, tutarlı bir tipografi sistemi oluşturmak için belirteçlerden tam olarak nasıl yararlanacağınızı açıklar.<br />
> Tipografi sisteminden en iyi şekilde yararlanmak için [Text bileşeni belgelerini](./?path=/docs/components-text--docs) de okumanızı öneririz.**

<IqvizyonCanvas>
  <Typography />
</IqvizyonCanvas>

## Nasıl kullanılır?

Tipografi stillerini kullanmak için herhangi bir bileşene özel bir stil oluşturmanız yeterlidir. `typographyStyles` öğesini ana `@iqvizyonui/react-components` paketinden içe aktarın ve özel stilinizde kullanın:

```tsx
import * as React from 'react';
import { makeStyles, typographyStyles } from '@iqvizyonui/react-components';

const useStyles = makeStyles({
  text: typographyStyles.title2,
});

export const Example = () => {
  const styles = useStyles();

  return <span className={styles.text}>Belirteçleri kullanan metin</span>;
};
```

<IqvizyonCanvas>
  <Example />
</IqvizyonCanvas>

## Belirteçleri birleştirme

Tipografi stillerinin yanı sıra tipografi belirteçlerini ayrı ayrı da kullanabilirsiniz:

```tsx
import * as React from 'react';
import { makeStyles, tokens } from '@iqvizyonui/react-components';

const useStyles = makeStyles({
  text: {
    fontFamily: tokens.fontFamilyMonospace,
    fontSize: tokens.fontSizeBase600,
    fontWeight: tokens.fontWeightBold,
    lineHeight: tokens.lineHeightBase600,
  },
});

export const Composing = () => {
  const styles = useStyles();

  return <span className={styles.text}>Yalnızca belirteçleri kullanan özel metin</span>;
};
```

<IqvizyonCanvas>
  <Composing />
</IqvizyonCanvas>

## Belirteçleri ve özel stilleri birlikte kullanma

Belirteçleri özel stillerle birlikte kullanmak için tipografi stillerini stil nesnesine yayın ve diğer CSS özellikleriyle birleştirin:

```tsx
import * as React from 'react';
import { makeStyles, typographyStyles, Text } from '@iqvizyonui/react-components';

const useStyles = makeStyles({
  title: typographyStyles.title2,

  paragraph: {
    ...typographyStyles.body1,
    letterSpacing: '0.0675em',
    fontStyle: 'italic',
  },
});

export const MixedStyles = () => {
  const styles = useStyles();

  return (
    <div>
      <Text as="h1" block className={styles.title}>
        Başlık 2 belirteçlerini kullanma
      </Text>

      <Text as="p" block className={styles.paragraph}>
        Gövde 1 belirteçlerini ve özelleştirilmiş stilleri kullanan bir paragrafım
      </Text>
    </div>
  );
};
```

<IqvizyonCanvas>
  <MixedStyles />
</IqvizyonCanvas>

</Tr>

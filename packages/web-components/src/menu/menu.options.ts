import { IqvizyonDesignSystem } from '../iqvizyon-design-system.js';

/**
 * The tag name for the menu element.
 *
 * @public
 */
export const tagName = `${IqvizyonDesignSystem.prefix}-menu` as const;
